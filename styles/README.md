# styles/

| `theme.css` | Brand colours, fonts, shadows (the design tokens) | You want to change a colour or font everywhere |
| `animations.css` | Every movement, transition and the focus ring | You want to add/adjust an animation |

Both are loaded by `app/globals.css`. You almost never touch `globals.css` itself.
